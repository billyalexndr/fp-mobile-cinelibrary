<?xml version="1.0" encoding="utf-8"?>
<!-- Contoh file XML untuk tombol yang sudah di-bookmark -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State saat tombol di-klik atau di-tap -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <!-- Background saat tombol di-klik -->
            <solid android:color="#FF5722" /> <!-- Ganti dengan warna untuk tombol di-klik saat sudah di-bookmark -->
            <!-- Tambahkan corner radius atau styling lainnya jika diinginkan -->
        </shape>
    </item>
    <!-- State normal -->
    <item>
        <shape android:shape="rectangle">
            <!-- Background saat tombol dalam keadaan normal -->
            <solid android:color="#FF9800" /> <!-- Ganti dengan warna untuk tombol normal saat sudah di-bookmark -->
            <!-- Tambahkan corner radius atau styling lainnya jika diinginkan -->
        </shape>
    </item>
</selector>
